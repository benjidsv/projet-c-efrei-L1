#ifndef PROJETC_MATH_H
#define PROJETC_MATH_H

int Max(int x, int y) {
    return x > y ? x : y;
}

int Min(int x, int y) {
    return x < y ? x : y;
}

int Abs(int x) {
    return x > 0 ? x : -x;
}

#endif //PROJETC_MATH_H
